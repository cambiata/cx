package print.mgr.controller.command;
import msignal.Signal.Signal0;
import mmvc.impl.Command;

/**
 * ...
 * @author 
 */
class SerialCommand extends Signal0
{

	public function new(commands:Array<Command>) 
	{
		var commands:Array<Command> = commands;
	}
	
}